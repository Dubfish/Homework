<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Homework" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="checkboxStyle">@style/Theme.Homework.CheckBox</item>
    </style>

    <style name="Theme.Homework.CheckBox" parent="Widget.Material3.CompoundButton.CheckBox">
        <item name="android:textColor">@color/withered_grass</item>
        <item name="buttonTint">@color/withered_grass</item>
    </style>

    <style name="Theme.Homework.ByeButton" parent="Widget.Material3.Button">
        <item name="backgroundTint">@color/withered_grass</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="Theme.Homework.HelloButton" parent="Widget.Material3.Button.OutlinedButton">
        <item name="android:textColor">@color/withered_grass</item>
        <item name="strokeColor">@color/withered_grass</item>
    </style>

</resources>
